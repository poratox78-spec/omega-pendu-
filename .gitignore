__pycache__/
dictee/learned_lex.json
cgram_pos.json
# wikitext brut mis en cache par build_glosses_wiktionnaire.py (réextraction) — la sortie glosses_wiktionnaire.json, elle, est versionnée
dictee/glosses_cache.json
# config locale de l'agent (session) — sauf les skills projet, versionnés
.claude/*
!.claude/skills/

# Lexique 4 — source maître (hors-repo par doctrine : 34 Mo ; fourni en .xz local / Drive de Rem)
Lexique4.tsv
Lexique4.tsv.xz

# dump Wiktextract kaikki.org (hors-repo : ~544 Mo ; CC BY-SA ; re-téléchargeable depuis
# kaikki.org/dictionary/French/kaikki.org-dictionary-French.jsonl) — source des build_wikt_*/build_argot/build_invariable
kaikki-fr.jsonl
kaikki*.jsonl

# sources brutes anglaises (hors-repo, re-téléchargeables) — Phase 2 EN. On committe seulement
# les assets DÉRIVÉS (lex_en.tsv, homophones_en.json, ngrams_*_en.json.gz) avec attribution.
#   kaikki-en.jsonl : 3,19 Go, CC BY-SA, kaikki.org/dictionary/English/kaikki.org-dictionary-English.jsonl
#   subtlex_us.json : SUBTLEX-US freq sous-titres (words/subtlex-word-frequencies, MIT wrapper)
#   count_1w.txt    : Norvig count_1w (Google Web Trillion Word Corpus), fréquence de secours
subtlex_us.json
count_1w.txt
#   cmudict.dict    : CMU Pronouncing Dictionary (BSD-2), secours phonologie ARPAbet->IPA
cmudict.dict
# maître EN complet (~30 Mo/13 Mo, régénérable) — on committe les .gz SCOPÉS (signal utile) à la place
dictee/lex_en.tsv
dictee/forms_en.tsv
# substrat du pendu EN (intermédiaires régénérables via build_lex4_en.py ; embarqués base64 dans le clone HTML)
dictee/lex4_en.json.gz
dictee/lex4_en.b64
# clone du pendu EN = app/omega-pendu-en.html — COMMITTÉ (servi par le site ; moteur anglais + UI
# joueur traduite ; régénérable via build_pendu_en.py, traduction UI dashboard en cours).

# bundle de test (build output, régénérable via dist/make_test_bundle.sh)
dist/omega-correcteur-dys-test.zip

# corpus UD French-GSD (hors-repo : 25 Mo ; CC BY-SA 4.0 ; re-téléchargeable depuis
# raw.githubusercontent.com/UniversalDependencies/UD_French-GSD/master/fr_gsd-ud-train.conllu)
data_local/

# intermédiaire d'extraction (régénérable)
dictee/pendu_fr_strings.json

# paquet de soumission Chrome Web Store (artefact de publication, régénérable :
# python3 extension/build_zip.py --store) — le zip du SITE, lui, est commité et gardé frais en CI
omega-correcteur-dys-store.zip
inventaire_i18n.tsv
