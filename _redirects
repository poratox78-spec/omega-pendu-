# RÉFÉRENCEMENT — www → apex : la redirection NE PEUT PAS se faire ici (mesuré 2026-08-13).
#
# Le commit précédent posait une règle « https://www.omegapendu.com/* → apex » : APRÈS déploiement,
# le www répondait TOUJOURS 200 sans rediriger, et ce fichier était SERVI tel quel en 200 —
# preuve que Cloudflare Pages ne l'interprète pas (les sources avec HÔTE ne sont pas supportées
# dans le _redirects de Pages ; un fichier interprété ne serait pas exposé publiquement).
#
# ✅ FAIT le 2026-08-13 (via le Chrome de Rem, à sa demande) ET VÉRIFIÉ EN LIGNE : la règle de zone
# existe (« Rediriger de WWW vers la racine », wildcard https://www.omegapendu.com/* →
# https://omegapendu.com/${1}, 301, query préservée). Mesuré après propagation : /, /en/, /en/?x=1,
# /pendable → 301 vers l'apex en UN saut. Reste à Rem : Search Console → rapport « Page avec
# redirection » → « Valider la correction » (idem « Erreur liée à des redirections »).
#
# (Archive du diagnostic) LA VRAIE VOIE était une règle DE ZONE, dashboard Cloudflare :
#    Compte Cloudflare → zone omegapendu.com → Rules → Redirect Rules → Create rule :
#      · When incoming requests match : Hostname equals www.omegapendu.com
#      · Then : Dynamic redirect, status 301,
#        expression : concat("https://omegapendu.com", http.request.uri.path)
#      · Preserve query string : oui
#    Puis dans Search Console : « Valider la correction » sur les rapports
#    « Page avec redirection » et « Erreur liée à des redirections » (dictee.html est déjà sain).
#
# Contexte (rapport Search Console) : www servait un site COMPLET en double (200, redirs=0) →
# toutes les variantes crawlées. zh/ et arbitrage en noindex = DÉLIBÉRÉ. Les canonical sont
# déjà posés partout — ils limitent la casse en attendant la règle de zone.
